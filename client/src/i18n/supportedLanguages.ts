// Canonical language registry now lives in @moons/shared. Re-exported here so
// existing imports of './supportedLanguages' continue to work unchanged.
export { SUPPORTED_LANGUAGES, SUPPORTED_LANGUAGE_CODES } from '@moons/shared'
export type { SupportedLanguageCode } from '@moons/shared'
